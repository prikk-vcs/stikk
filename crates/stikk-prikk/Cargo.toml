[package]
name        = "stikk-prikk"
description = "The prikk seam: the only code in stikk that talks to prikk. CLI backend, version handshake, and presence-only key readiness."

version.workspace       = true
edition.workspace       = true
rust-version.workspace  = true
license.workspace       = true
authors.workspace       = true
repository.workspace    = true

[dependencies]
stikk-model = { workspace = true }

[lints]
workspace = true
