[package]
name        = "stikk-state"
description = "stikk's own durable data: config, sessions, recents. Lives in user scope, never inside a repository, and is never authority."

version.workspace       = true
edition.workspace       = true
rust-version.workspace  = true
license.workspace       = true
authors.workspace       = true
repository.workspace    = true

[dependencies]
stikk-model = { workspace = true }

[lints]
workspace = true
